{
  "name": "simple-cicd-pipeline",
  "version": "1.0.0",
  "description": "Simple web app for CI/CD learning",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest",
    "build": "mkdir -p dist && cp *.js dist/ && cp package*.json dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "supertest": "^6.1.6"
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": ["/node_modules/", "/dist/"]
  }
}
